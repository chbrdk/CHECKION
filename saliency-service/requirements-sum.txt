# CHECKION Saliency Service – SUM backend (website/UI)
# Same API as main.py; install SUM’s deps separately (see Dockerfile.sum)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
numpy
pillow
scipy
