# AUDION Journey → CREATION screen brief (cross-repo)

- **CREATION** implements `POST /api/v1/journey-screen-brief` and documents the contract in `CREATION/knowledge/journey-persona-screen-pipeline.md`.
- **AUDION** `apps/figma-plugin2` calls it via `CREATION_JOURNEY_SCREEN_BRIEF_PATH` in `config/urls.ts`; plugin notes: `apps/figma-plugin2/knowledge/journey-screen-brief-plugin.md`.
