/**
 * PDF font families — react-pdf built-in Helvetica (full glyph coverage, no custom embed).
 * Weight contrast via {@link PDF_TYPE_WEIGHT} in `pdf-typography.ts` (bold vs regular).
 */

export const PDF_FONT_FAMILIES = {
    body: 'Helvetica',
    headline: 'Helvetica',
} as const;
