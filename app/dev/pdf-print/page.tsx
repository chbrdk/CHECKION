import { PrintPreviewPlayground } from '@/components/pdf/preview/PrintPreviewPlayground';

export const metadata = {
    title: 'PDF Print Preview | CHECKION Dev',
    description: 'Interactive browser preview for CHECKION PDF print layout',
};

export default function DevPdfPrintPreviewPage() {
    return <PrintPreviewPlayground />;
}
